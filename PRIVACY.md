# 源码脱敏说明

本工程可直接用于公开分享：项目自身代码使用中性的 `org.huhobot.monitor` 命名空间，插件元数据不包含个人姓名，源码、配置与文档不记录本机用户名、绝对路径、联系方式或密钥。

源码中保留的 `cn.huohuas001.bot.*` 与 `cn.huohuas001.huhobotPenguin.*` 属于 HuHoBot 上游公开 API 标识，是编译和运行兼容所必需，并非本工程作者信息。

工程只保留 `docs/preview.png` 一张公开预览。`build/`、`.gradle/`、IDE 配置、运行日志、本地服务器目录和用户导入的自定义底图均由 `.gitignore` 排除，不应随源码发布。
